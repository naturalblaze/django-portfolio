"""App configuration for portfolio_blog app"""

from django.apps import AppConfig


class PortfolioBlogConfig(AppConfig):
    """Django app config for portfolio_blog."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "portfolio_blog"
