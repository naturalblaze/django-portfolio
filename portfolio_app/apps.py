"""App configuration for portfolio_app app"""

from django.apps import AppConfig


class PortfolioBlogConfig(AppConfig):
    """Django app config for portfolio_app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "portfolio_app"
